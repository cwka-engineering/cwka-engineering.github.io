_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
Gemfile.lock

# Source transcripts and archives (large, not for publication)
ingest/transcripts/*.txt
ingest/transcripts/*.docx
ingest/archive/**/*.txt
ingest/archive/**/*.docx

# Generated corpus files (rebuilt by CI)
scripts/corpus.json
scripts/corpus_prompt.txt

# Cloudflare Worker dependencies
chatbot-worker/node_modules/

# Local reference documents (not for publication)
chatbot-reference.docx

